# Worktree Feature Integration: Back to Main (EN)

## Prerequisites
- You have completed features in separate worktrees
- The features are committed and working in their respective branches
- You are in the main project directory, on the original branch from which you created the worktrees
- Stop Claude Code in each separate terminal to avoid confusion
- Run Claude Code in the main project directory on the original branch

## Step 1: Ask Claude Code to Handle Integration

"""""
I have two features developed in parallel worktrees that I need to integrate:
- feature/data-export (export functionality)
- feature/analytics-dashboard (analytics functionality)

Please help me integrate these features:
1. Create a new integration branch called "integration/export-analytics"
2. Merge feature/data-export into the integration branch
3. Merge feature/analytics-dashboard into the integration branch
4. Resolve any merge conflicts that arise
5. Test that both features work together
6. Run all tests to ensure nothing is broken

I want to integrate these safely before merging to main.
"""""

## Result
Your parallel features are now integrated in your main branch, and all worktrees are clean. Test your application to ensure everything works correctly.

---

# Fusion de Funciones de Worktree: Volver a la Pagina Principal (ES)

## Requisitos Previos
- Ha completado funciones en arboles de trabajo separados
- Las funciones estan confirmadas y funcionando en sus respectivas ramas
- Se encuentra en el directorio principal del proyecto, en la rama original a partir de la cual creo los arboles de trabajo
- Detenga Claude Code en cada uno de los terminales separados para no confundirse
- Ejecute Claude Code en el directorio principal del proyecto en la rama original

## Paso 1: Pida a Claude Code que se Encargue de la Integracion

"""""
I have two features developed in parallel worktrees that I need to integrate:
- feature/data-export (export functionality)
- feature/analytics-dashboard (analytics functionality)

Please help me integrate these features:
1. Create a new integration branch called "integration/export-analytics"
2. Merge feature/data-export into the integration branch
3. Merge feature/analytics-dashboard into the integration branch
4. Resolve any merge conflicts that arise
5. Test that both features work together
6. Run all tests to ensure nothing is broken

I want to integrate these safely before merging to main.
"""""

## Resultado
Sus funciones paralelas estan ahora integradas en su rama principal, y todos los arboles de trabajo estan limpios. Pruebe su aplicacion para asegurarse de que todo funciona correctamente.
