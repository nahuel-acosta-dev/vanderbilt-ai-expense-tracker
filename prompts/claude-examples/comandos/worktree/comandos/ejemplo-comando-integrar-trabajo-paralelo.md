# Integrate Parallel Work Command (EN)

Possible real-code location: `.claude/commands/integrate-parallel-work.md`

I have features developed in parallel worktrees that I need to integrate: $ARGUMENTS

Please help me integrate these features:
1. Create a new integration branch called "integration/parallel-features"
2. For each feature name provided, merge the branch `feature/[feature-name]` into the integration branch
3. Resolve any merge conflicts that arise
4. Test that all features work together
5. Run all tests to ensure nothing is broken
6. Once integration is successful, merge to main and clean up branches

I want to integrate these safely before merging to main.

---

# Comando Integrar Trabajo Paralelo (ES)

Posible ubicacion real en el codigo: `.claude/commands/integrate-parallel-work.md`

Tengo funcionalidades desarrolladas en worktrees paralelos que necesito integrar: $ARGUMENTS

Por favor ayudame a integrar estas funcionalidades:
1. Crear una nueva rama de integracion llamada "integration/parallel-features"
2. Para cada nombre de funcionalidad provisto, mergear la rama `feature/[feature-name]` en la rama de integracion
3. Resolver cualquier conflicto de merge que surja
4. Probar que todas las funcionalidades funcionen juntas
5. Ejecutar todos los tests para asegurar que nada se rompio
6. Una vez que la integracion sea exitosa, mergear a main y limpiar ramas

Quiero integrar esto de forma segura antes de mergear a main.
